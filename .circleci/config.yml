version: 2 # CircleCI version
jobs:
  build:
    machine: true # Use a Linux VM instead of docker environment
    working_directory: ~/repo
    steps:
      - checkout
      - run: ./.circleci.sh
      - run: cd ./university-agent && mvn sonar:sonar \ -Dsonar.projectKey=tijn167_StudyBitsClone \ -Dsonar.organization=tijn167-github \-Dsonar.host.url=https://sonarcloud.io \ -Dsonar.login=3e3522a517ba920fe757ea8cf5e9255350f270e5
