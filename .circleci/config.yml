    
version: 2 # CircleCI version
jobs:
  build:
    machine: true # Use a Linux VM instead of docker environment
    working_directory: ~/repo
    steps:
      - checkout
      - run:  .circleci.sh
      #- run: cd ./university-agent mvn sonar:sonar \-Dsonar.projectKey=tijn167_StudyBitsClone \-Dsonar.organization=tijn167-github \-Dsonar.host.url=https://sonarcloud.io \-Dsonar.login=f190fe8b2de543642d433b425cf7dcca7d7b9e45
